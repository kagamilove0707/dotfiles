# ===ls関連===
# lsを色付きに
alias ls="ls -C"
# ls -aのalias(チルダで終わるファイルは非表示)
alias lsa="ls -Ba"

# ===popd/pushd関連===
alias pd="popd"
alias 2pd="popd && popd"
alias 3pd="popd && popd && popd"

# ===git関連===
alias gst="git status -bs"
alias gct="git commit"
alias gctm="gct -m"
alias gad="git add"
